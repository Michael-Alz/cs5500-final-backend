# ===========================
# 5500 Backend - Production Example Env
# NOTE: Copy to your server (e.g., /opt/classconnect/.env.prod) and fill real secrets.
# Keep this file OUT of version control in real deployments.
# ===========================

# Application
APP_NAME=5500 Backend
APP_ENV=prod
PORT=8000

# Database
# If you run Postgres as a container on the same EC2 host, point to the service/container name
# inside the Docker network (not localhost). Example with compose service name "database":
# postgresql+psycopg://class_connect_user:class_connect_password@database:5432/class_connect_db
# If you run Postgres bare-metal on the EC2 host, use the host IP (or host.docker.internal on Linux
# with the right daemon config).
DATABASE_URL=postgresql+psycopg://class_connect_user:class_connect_password@database:5432/class_connect_db
# Postgres container credentials (used by docker-compose.prod.yml)
POSTGRES_USER=class_connect_user
POSTGRES_PASSWORD=class_connect_password
POSTGRES_DB=class_connect_db

# JWT (set to strong, random values)
JWT_SECRET=change_me_in_prod
JWT_EXPIRE_HOURS=24

# CORS allowed origins (comma-separated JSON array)
# Add your frontend domain(s) and any admin UI domains.
CORS_ORIGINS=["https://your-frontend.example.com"]

# Public app URL (used for QR codes or public links)
PUBLIC_APP_URL=https://your-frontend.example.com

# Host port mapping (host:container)
HOST_PORT=8000

# Optional: logging verbosity (info, warning, error)
LOG_LEVEL=info

# Admin maintenance password (set to a strong value; disable exposed admin UIs in prod)
MAINTENANCE_ADMIN_PASSWORD=change_me_in_prod

# Optional: explicit system default activity UUID (leave blank to auto-pick)
SYSTEM_DEFAULT_ACTIVITY_ID=

# AI Recommendations Service Configuration (omit if not used in prod)
OPENROUTER_API_KEY=
OPENROUTER_DEFAULT_MODEL=anthropic/claude-3.5-sonnet
OPENROUTER_API_BASE=https://openrouter.ai/api/v1/chat/completions
